1. extend Choose
2. Research on Random Walk


How To Run the Compiler in Terminal:
1. go to "pwhile" directory

2. to compile:
> make

3. set up ruby frontend:
> #!/usr/bin/env ruby
> chmod +x compile

4. to run the compiler:
> ./compile filename.pw

5. to run the output program: (make sure you have Python and TensorFlow installed)
> python filename.py